# webscraper
Небольшой Python-скрипт для парсинга веб-страниц. Он извлекает заголовки статей и ссылки со страницы и сохраняет их в CSV-файл.  Скрипт работает с большинством сайтов, анализируя распространённую HTML-структуру (article, h1, h2, h3).
